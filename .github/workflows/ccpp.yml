name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: change directory
      working-directory: minimatrix/software/matrix-simpleclock/testcases
    - name: make
      run: make
    - name: check
      run: ./runtestcases
